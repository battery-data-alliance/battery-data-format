{
  "$defs": {
    "Artifact": {
      "additionalProperties": false,
      "description": "A runnable or vendor-produced protocol/data file linked to the record.",
      "properties": {
        "byte_size": {
          "description": "File size in bytes.",
          "minimum": 0,
          "type": "integer"
        },
        "conforms_to": {
          "description": "IRI or label of the format specification the artifact conforms to.",
          "type": "string"
        },
        "format": {
          "description": "Artifact format label (e.g. 'pybamm', 'neware-xml', 'biologic-mps').",
          "minLength": 1,
          "type": "string"
        },
        "generated": {
          "description": "Locators of artifacts generated from this one (e.g. an executed program exported from the source protocol).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "generated_from": {
          "description": "Locator of the artifact this one was generated from.",
          "type": "string"
        },
        "locator": {
          "description": "Where the artifact lives: a URL, DOI, or path within the linked dataset.",
          "minLength": 1,
          "type": "string"
        },
        "media_type": {
          "description": "IANA media type of the file.",
          "type": "string"
        },
        "role": {
          "description": "How the artifact relates to the record: the authored protocol, the executed program, a vendor export, or a simulation input.",
          "enum": [
            "source_protocol",
            "executed_protocol",
            "vendor_export",
            "simulation_input",
            "other"
          ],
          "type": "string"
        },
        "sha256": {
          "description": "SHA-256 hash of the file.",
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        }
      },
      "required": [
        "role",
        "format",
        "locator"
      ],
      "type": "object"
    },
    "Funding": {
      "additionalProperties": false,
      "description": "Funding project (grant) under which the record was produced. Maps to schema:funding / schema:Grant in JSON-LD.",
      "properties": {
        "acronym": {
          "description": "Project acronym.",
          "type": "string"
        },
        "funder": {
          "additionalProperties": false,
          "description": "Organization that awarded the grant.",
          "properties": {
            "name": {
              "description": "Funder name (e.g. 'European Commission').",
              "minLength": 1,
              "type": "string"
            },
            "same_as": {
              "description": "External identifier IRI for the funder (e.g. a ROR or DOI funder IRI).",
              "format": "uri",
              "type": "string"
            },
            "type": {
              "const": "Organization",
              "description": "JSON-LD type discriminator."
            },
            "url": {
              "description": "Funder homepage URL.",
              "format": "uri",
              "type": "string"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        },
        "id": {
          "description": "Resolvable IRI of the grant (e.g. a CORDIS project IRI).",
          "format": "uri",
          "type": "string"
        },
        "identifier": {
          "description": "Grant number as assigned by the funder (e.g. '101069765').",
          "minLength": 1,
          "type": "string"
        },
        "name": {
          "description": "Project title.",
          "type": "string"
        },
        "program": {
          "description": "Funding programme or call (e.g. 'Horizon Europe').",
          "type": "string"
        },
        "type": {
          "const": "Grant",
          "description": "JSON-LD type discriminator."
        }
      },
      "required": [
        "identifier"
      ],
      "type": "object"
    },
    "Organization": {
      "additionalProperties": false,
      "description": "An organization referenced by name, with an optional canonical BattINFO organization IRI.",
      "properties": {
        "name": {
          "description": "Organization name.",
          "minLength": 1,
          "type": "string"
        },
        "same_as": {
          "description": "External identifier IRI for this organization (e.g. ROR or Wikidata).",
          "format": "uri",
          "type": "string"
        },
        "type": {
          "const": "Organization",
          "description": "JSON-LD type discriminator."
        },
        "url": {
          "description": "Homepage URL.",
          "format": "uri",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "Person": {
      "additionalProperties": false,
      "description": "A person, in schema.org Person terms; an ORCID may be given in same_as.",
      "properties": {
        "affiliation": {
          "$ref": "#/$defs/Organization",
          "description": "Organization the person is affiliated with."
        },
        "email": {
          "description": "Contact e-mail address.",
          "format": "email",
          "type": "string"
        },
        "family_name": {
          "description": "Family (last) name.",
          "minLength": 1,
          "type": "string"
        },
        "given_name": {
          "description": "Given (first) name.",
          "minLength": 1,
          "type": "string"
        },
        "name": {
          "description": "Full name.",
          "minLength": 1,
          "type": "string"
        },
        "same_as": {
          "description": "External identifier IRI, typically an ORCID (https://orcid.org/...).",
          "format": "uri",
          "type": "string"
        },
        "type": {
          "const": "Person",
          "description": "JSON-LD type discriminator."
        },
        "url": {
          "description": "Personal or institutional web page.",
          "format": "uri",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "Quantity": {
      "additionalProperties": false,
      "anyOf": [
        {
          "required": [
            "value"
          ]
        },
        {
          "required": [
            "min_value"
          ]
        },
        {
          "required": [
            "max_value"
          ]
        }
      ],
      "description": "A bare value+unit pair.",
      "properties": {
        "max_value": {
          "description": "Upper bound of a window quantity; field names align with modules/common/quantity.schema.json.",
          "type": "number"
        },
        "min_value": {
          "description": "Lower bound of a window quantity (e.g. a 2.5-4.2 V voltage window); field names align with modules/common/quantity.schema.json.",
          "type": "number"
        },
        "unit": {
          "description": "Unit symbol (e.g. 'V', 'mA', 'h').",
          "minLength": 1,
          "type": "string"
        },
        "value": {
          "description": "Numeric value.",
          "type": "number"
        }
      },
      "required": [
        "unit"
      ],
      "type": "object"
    },
    "ShortId": {
      "description": "Short human-friendly identifier derived from the record IRI (Crockford base32, 6-16 characters).",
      "pattern": "^[0-9a-hjkmnp-tv-z]{6,16}$",
      "type": "string"
    },
    "SpecIri": {
      "pattern": "^https://w3id\\.org/battinfo/(?:spec|material-spec|electrode-spec|separator-spec|electrolyte-spec|current-collector-spec|housing-spec)/[0-9a-hjkmnp-tv-z]{4}(?:-[0-9a-hjkmnp-tv-z]{4}){3}$",
      "type": "string"
    },
    "Step": {
      "additionalProperties": false,
      "description": "One typed step of the protocol, or a repeated group of child steps.",
      "properties": {
        "count": {
          "description": "Repeat count when this step is a repeated group.",
          "minimum": 1,
          "type": "integer"
        },
        "description": {
          "description": "Free-text description of the step.",
          "type": "string"
        },
        "direction": {
          "description": "Current direction or role of the step.",
          "enum": [
            "charge",
            "discharge",
            "hold",
            "rest",
            "none"
          ],
          "type": "string"
        },
        "duration": {
          "$ref": "#/$defs/Quantity",
          "description": "Fixed step duration."
        },
        "mode": {
          "description": "Electrochemical mode of the step (constant current, constant voltage, rest, EIS, ...).",
          "enum": [
            "cc",
            "cv",
            "cccv",
            "cp",
            "cr",
            "rest",
            "eis",
            "scan",
            "group"
          ],
          "type": "string"
        },
        "period": {
          "additionalProperties": true,
          "description": "Step-level data-recording cadence overriding the protocol default.",
          "type": "object"
        },
        "setpoints": {
          "additionalProperties": {
            "$ref": "#/$defs/Quantity"
          },
          "description": "Controlled setpoints for the step (e.g. current, c_rate, voltage, power), each a value+unit quantity.",
          "type": "object"
        },
        "step_id": {
          "description": "Identifier assigned to this step in the test program/schedule. Corresponds to BDF step_id. Not constrained to zero-based, contiguous, monotonic, or unique values.",
          "type": "integer"
        },
        "steps": {
          "description": "Child steps of a repeated group.",
          "items": {
            "$ref": "#/$defs/Step"
          },
          "type": "array"
        },
        "tags": {
          "description": "Free-form labels for querying (e.g. 'formation', 'checkup').",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "temperature": {
          "$ref": "#/$defs/Quantity",
          "description": "Ambient temperature setpoint for the step."
        },
        "termination": {
          "description": "Conditions that end the step.",
          "items": {
            "$ref": "#/$defs/Termination"
          },
          "type": "array"
        }
      },
      "required": [
        "mode"
      ],
      "type": "object"
    },
    "Termination": {
      "additionalProperties": false,
      "description": "A condition that ends a protocol step.",
      "properties": {
        "direction": {
          "description": "Whether the step ends when the quantity falls below or rises above the threshold, or when the duration has elapsed.",
          "enum": [
            "below",
            "above",
            "elapsed"
          ],
          "type": "string"
        },
        "quantity": {
          "description": "Quantity monitored for the termination condition.",
          "enum": [
            "voltage",
            "current",
            "c_rate",
            "capacity",
            "duration"
          ],
          "type": "string"
        },
        "unit": {
          "description": "Unit of the threshold value.",
          "minLength": 1,
          "type": "string"
        },
        "value": {
          "description": "Threshold value.",
          "type": "number"
        }
      },
      "required": [
        "quantity",
        "value",
        "unit"
      ],
      "type": "object"
    },
    "UnixTime": {
      "description": "Unix timestamp in whole seconds since 1970-01-01T00:00:00Z.",
      "minimum": 0,
      "type": "integer"
    }
  },
  "$id": "https://w3id.org/battinfo/schema/test-protocol.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Canonical BattINFO reusable test protocol metadata. The descriptive layer is an ordered, typed `method` of steps (queryable, not executed); the actionable layer is `artifacts`, links to runnable protocol files. Humans author the method as PyBaMM-style strings which are parsed into `method` on ingest.",
  "license": "https://www.apache.org/licenses/LICENSE-2.0",
  "properties": {
    "artifacts": {
      "description": "Actionable-layer links to runnable protocol files.",
      "items": {
        "$ref": "#/$defs/Artifact"
      },
      "type": "array"
    },
    "conditions": {
      "additionalProperties": {
        "$ref": "#/$defs/Quantity"
      },
      "description": "Planned protocol-level conditions, as a map of condition name to a {value, unit} quantity. Recommended keys: ambient_temperature, upper_voltage_limit, lower_voltage_limit, c_rate. As-run values or deviations are recorded on the test record.",
      "type": "object"
    },
    "contributor": {
      "description": "People who contributed this record to the platform (attribution/provenance). Each is a schema:Person; an ORCID may be given in same_as. Distinct from data authorship (creators).",
      "items": {
        "$ref": "#/$defs/Person"
      },
      "minItems": 1,
      "type": "array"
    },
    "facets": {
      "additionalProperties": true,
      "description": "Derived rollup index of the method for cheap filtering. Computed from `method`; never authored by hand.",
      "type": "object"
    },
    "funding": {
      "$ref": "#/$defs/Funding",
      "description": "Funding project (grant) under which the record was produced."
    },
    "license": {
      "description": "License under which this record and its data are released, as an SPDX license identifier (e.g. \"cc-by-4.0\") or a license URL. Stamped from the workspace default (ws.license) and emitted as dcterms:license in JSON-LD.",
      "minLength": 1,
      "type": "string"
    },
    "method": {
      "description": "Ordered, typed descriptive steps. Canonical structured form of the procedure.",
      "items": {
        "$ref": "#/$defs/Step"
      },
      "type": "array"
    },
    "notes": {
      "description": "Free-text notes carried with the record.",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "provenance": {
      "additionalProperties": false,
      "description": "Where the information in this record came from: source type, file or URL, citation, and retrieval time.",
      "properties": {
        "battinfo_version": {
          "description": "Version of the battinfo library that wrote this record (semantic versioning); stamped on save.",
          "pattern": "^\\d+\\.\\d+\\.\\d+(-[A-Za-z0-9.-]+)?$",
          "type": "string"
        },
        "citation": {
          "description": "Resolvable citation target to use for attribution, typically a DOI URL.",
          "format": "uri",
          "type": "string"
        },
        "citation_doi": {
          "description": "DOI of the source publication (bare '10.xxxx/...' form). Prefer provenance.citation with a DOI resolver URL.",
          "pattern": "^10\\.\\d{4,9}/[-._;()/:A-Za-z0-9]+$",
          "type": "string"
        },
        "comment": {
          "description": "Free-text provenance comment.",
          "type": "string"
        },
        "curated_by": {
          "description": "Name or identifier of the person who curated this record.",
          "type": "string"
        },
        "derived_from_artifact": {
          "description": "Role or locator of the artifact this method was derived from.",
          "type": "string"
        },
        "file_hash": {
          "description": "SHA-256 hash of the source file, for integrity verification.",
          "pattern": "^[A-Fa-f0-9]{64}$",
          "type": "string"
        },
        "retrieved_at": {
          "$ref": "#/$defs/UnixTime",
          "description": "Unix timestamp (seconds since epoch, UTC) when the source was retrieved or the record was captured."
        },
        "source_file": {
          "description": "Filename or path of the source file, when the record was derived from a file.",
          "type": "string"
        },
        "source_name": {
          "description": "Human-readable name of the source (document title, instrument, lab, or system).",
          "type": "string"
        },
        "source_type": {
          "description": "Kind of source this record was created from.",
          "enum": [
            "manual",
            "lab",
            "simulation",
            "import",
            "other"
          ],
          "type": "string"
        },
        "source_url": {
          "anyOf": [
            {
              "format": "uri",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "URL of the source document or landing page, when available."
        },
        "workflow_version": {
          "description": "Version of the ingest/curation workflow that produced this record.",
          "type": "string"
        }
      },
      "required": [
        "source_type",
        "retrieved_at"
      ],
      "type": "object"
    },
    "record": {
      "additionalProperties": false,
      "description": "Protocol-level default data-recording cadence (PyBaMM `period` / aurora `record`).",
      "properties": {
        "current_mA": {
          "description": "Record a data point on a current change of this many milliamps.",
          "exclusiveMinimum": 0,
          "type": "number"
        },
        "time_s": {
          "description": "Record a data point every this many seconds.",
          "exclusiveMinimum": 0,
          "type": "number"
        },
        "voltage_V": {
          "description": "Record a data point on a voltage change of this many volts.",
          "exclusiveMinimum": 0,
          "type": "number"
        }
      },
      "type": "object"
    },
    "safety": {
      "additionalProperties": false,
      "description": "Global instrument hard limits, distinct from per-step termination.",
      "properties": {
        "delay_s": {
          "description": "Grace period in seconds before the safety limits are enforced.",
          "type": [
            "number",
            "null"
          ]
        },
        "max_capacity_mAh": {
          "description": "Hard capacity limit in milliamp-hours.",
          "type": [
            "number",
            "null"
          ]
        },
        "max_current_mA": {
          "description": "Hard upper current limit in milliamps.",
          "type": [
            "number",
            "null"
          ]
        },
        "max_temperature_degC": {
          "type": [
            "number",
            "null"
          ]
        },
        "max_voltage_V": {
          "description": "Hard upper voltage limit in volts; the instrument aborts beyond it.",
          "type": [
            "number",
            "null"
          ]
        },
        "min_current_mA": {
          "description": "Hard lower current limit in milliamps.",
          "type": [
            "number",
            "null"
          ]
        },
        "min_temperature_degC": {
          "type": [
            "number",
            "null"
          ]
        },
        "min_voltage_V": {
          "description": "Hard lower voltage limit in volts.",
          "type": [
            "number",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "schema_version": {
      "description": "Version of the record schema this document conforms to (semantic versioning); stamped by the library on save.",
      "pattern": "^\\d+\\.\\d+\\.\\d+(-[A-Za-z0-9.-]+)?$",
      "type": "string"
    },
    "test_spec": {
      "additionalProperties": false,
      "description": "Identity and classification of the protocol.",
      "properties": {
        "description": {
          "description": "Free-text description of the procedure.",
          "type": "string"
        },
        "id": {
          "$ref": "#/$defs/SpecIri"
        },
        "identifier": {
          "description": "Local identifier for the protocol (e.g. a lab protocol code).",
          "minLength": 1,
          "type": "string"
        },
        "kind": {
          "description": "Kind of test the protocol implements.",
          "enum": [
            "cycling",
            "capacity_check",
            "rate_capability",
            "hppc",
            "ici",
            "gitt",
            "dcir",
            "eis",
            "impedance",
            "calendar_ageing",
            "formation",
            "rpt",
            "quasi_ocv",
            "field",
            "duty_cycle",
            "wltp",
            "nedc",
            "sem",
            "characterization",
            "other"
          ],
          "type": "string"
        },
        "name": {
          "description": "Human-readable protocol name.",
          "minLength": 1,
          "type": "string"
        },
        "protocol_url": {
          "description": "URL of the protocol document.",
          "format": "uri",
          "type": "string"
        },
        "short_id": {
          "$ref": "#/$defs/ShortId"
        },
        "version": {
          "description": "Protocol version label.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "name",
        "kind"
      ],
      "type": "object"
    }
  },
  "required": [
    "schema_version",
    "test_spec",
    "provenance"
  ],
  "title": "BattINFO Test Protocol",
  "type": "object"
}
